# Badger — Python dependencies
# Install with:  pip install -r requirements.txt

# Core app
streamlit==1.38.0
pandas==2.2.2
numpy==1.26.4

# Google Sheets (live connection — pick the path you use; both are listed)
st-gsheets-connection==0.0.4      # Streamlit's gsheets connection wrapper
gspread==6.1.2                    # Direct Sheets API access via service account
google-auth==2.33.0
google-auth-oauthlib==1.2.1

# Calendar + charts
streamlit-calendar==1.3.0
plotly==5.23.0

# Slack (Badger's voice)
slack_sdk==3.31.0

# PDF / report generation
fpdf2==2.7.9
reportlab==4.2.2

# Dates & utilities
python-dateutil==2.9.0
rapidfuzz==3.9.6                  # fuzzy name matching to the organigram
openpyxl==3.1.5                  # Excel export support
